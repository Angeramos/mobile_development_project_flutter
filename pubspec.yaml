name: mobile_development_project_flutter
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Paquetes extra
  get: ^4.6.6        # Para GetX (estado, rutas, DI)
  equatable: ^2.0.5  # Para comparar objetos y estados más fácil

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
